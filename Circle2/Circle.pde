class Circle {
  float x, y;
  float radius;
  
  // Default constructor
  Circle() {
    x = 0;
    y = 0;
    radius = 10;
  }

  // Constructor with just the radius
  Circle(float radius) {
    this.radius = radius;  
  }
}
